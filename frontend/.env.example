VITE_RPC_URL = "https://mainnet.helius-rpc.com/?api-key=YOUR_HELIUS_API_KEY"

# On-chain vault settings
# Set to "true" to use the on-chain smart contract vault
# Set to "false" or remove to use the legacy treasury wallet system
VITE_USE_ONCHAIN_VAULT = "true"

# Solana network: "devnet" or "mainnet"
VITE_SOLANA_NETWORK = "devnet"
